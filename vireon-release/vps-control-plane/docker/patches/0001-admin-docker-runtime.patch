diff --git a/veiron-release/vps-control-plane/admin-server/src/config.rs b/veiron-release/vps-control-plane/admin-server/src/config.rs
index 3fb072d..docker01 100644
--- a/veiron-release/vps-control-plane/admin-server/src/config.rs
+++ b/veiron-release/vps-control-plane/admin-server/src/config.rs
@@ -1,4 +1,5 @@
 use serde::Deserialize;
+use std::env;
 use std::fs;
 use std::net::IpAddr;
 use std::path::{Path, PathBuf};
@@ -45,11 +46,14 @@ impl AdminConfig {
     }
 
     pub fn validate(&self) -> Result<(), String> {
+        let docker_mode = env::var("VEIRON_DEPLOYMENT_MODE")
+            .map(|value| value.eq_ignore_ascii_case("docker"))
+            .unwrap_or(false);
         let bind_ip: IpAddr = self
             .bind_host
             .parse()
             .map_err(|_| "bind_host must be a literal loopback IP".to_owned())?;
-        if !bind_ip.is_loopback() {
+        if !bind_ip.is_loopback() && !docker_mode {
             return Err("admin service must bind to a loopback address".to_owned());
         }
         if self.network_id != "veiron-mainnet-candidate" {
@@ -60,8 +66,12 @@ impl AdminConfig {
         }
         if !self.local_rpc_url.starts_with("http://127.0.0.1:")
             && !self.local_rpc_url.starts_with("http://[::1]:")
+            && !(docker_mode
+                && self
+                    .local_rpc_url
+                    .starts_with("http://veiron-rpc:"))
         {
-            return Err("local_rpc_url must use loopback HTTP".to_owned());
+            return Err("local_rpc_url must use loopback HTTP or the Docker-internal veiron-rpc service".to_owned());
         }
         if let Some(url) = &self.controller_url {
             if !url.is_empty() && !url.starts_with("https://") {
diff --git a/veiron-release/vps-control-plane/admin-server/src/app.rs b/veiron-release/vps-control-plane/admin-server/src/app.rs
index e79fe80..docker01 100644
--- a/veiron-release/vps-control-plane/admin-server/src/app.rs
+++ b/veiron-release/vps-control-plane/admin-server/src/app.rs
@@ -16,5 +16,6 @@ use axum::{Json, Router};
 use reqwest::Client;
 use serde_json::{json, Value};
+use std::env;
 use std::fs;
 use std::path::Path;
 use std::process::Command;
@@ -392,6 +393,16 @@ async fn collect_local_report(state: &AdminState) -> NodeReport {
         get_json(&state.client, &mempool_url),
         get_json(&state.client, &indexer_url),
     );
+    let services = if docker_mode() {
+        ServiceStates {
+            node: json_service_state(&p2p),
+            rpc: json_service_state(&status),
+            indexer_timer: json_service_state(&indexer),
+            admin: "active".to_owned(),
+        }
+    } else {
+        service_states()
+    };
     NodeReport {
         network_id: state.config.network_id.clone(),
         node_name: state.config.node_name.clone(),
@@ -403,7 +414,7 @@ async fn collect_local_report(state: &AdminState) -> NodeReport {
             state.config.p2p_port
         ),
         reported_at_unix_seconds: unix_seconds(),
-        services: service_states(),
+        services,
         status,
         sync,
         p2p,
@@ -432,5 +443,19 @@ fn service_states() -> ServiceStates {
     }
 }
 
+fn docker_mode() -> bool {
+    env::var("VEIRON_DEPLOYMENT_MODE")
+        .map(|value| value.eq_ignore_ascii_case("docker"))
+        .unwrap_or(false)
+}
+
+fn json_service_state(payload: &Value) -> String {
+    if payload.get("error").is_some() {
+        "inactive".to_owned()
+    } else {
+        "active".to_owned()
+    }
+}
+
 fn systemd_state(unit: &str) -> String {
     if !cfg!(target_os = "linux") {
         return "not-applicable".to_owned();
